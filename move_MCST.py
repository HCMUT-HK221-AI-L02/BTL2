# Định nghĩa giải thuật đưa ra move dựa trên Monte Carlo
def move(prev_board, board, player, remain_time_x, remain_time_o):
    # Tạo board trạng thái hiện tại
    # Gọi giải thuật chọn move
    # xuất move được chọn ra
    return